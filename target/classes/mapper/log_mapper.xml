<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="logService">
	<insert id="insertErrorLog" parameterType="com.taihe.schedule.entity.ErrorLogInfo">
		insert into erroinfo
			(info,
			inserttime,
			timeout,
			sign)
		values(
			#{failedInfo,
			jdbcType=VARCHAR},
			#{dataTime},
			#{buziType},
			#{buziId})
	</insert>
	
	
</mapper>